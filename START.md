# 🚀 Инструкция по запуску

## Вариант 1: Docker (рекомендуется)

### Шаг 1: Запуск базы данных

```bash
docker-compose up -d postgres
```

Проверьте что база запустилась:
```bash
docker-compose ps
```

### Шаг 2: Запуск Backend

```bash
go run main.go
```

Backend автоматически:
- ✅ Подключится к базе данных
- ✅ Выполнит миграции (создаст таблицы)
- ✅ Создаст дефолтные уроки

### Шаг 3: Запуск Frontend

```bash
cd frontend
npm install
npm run dev
```

## Вариант 2: Весь стек в Docker

```bash
# Запуск всего (БД + Backend)
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend
```

## Проверка работы

1. **Backend:** `http://localhost:8080`
2. **Frontend:** `http://localhost:3000`
3. **База данных:** `localhost:5432`

## Первый запуск

1. Запустите базу данных: `docker-compose up -d postgres`
2. Запустите backend: `go run main.go`
3. Запустите frontend: `cd frontend && npm run dev`
4. Откройте браузер: `http://localhost:3000`
5. Зарегистрируйтесь и начните изучать!

## Изменение роли на учителя

Подключитесь к базе данных:

```bash
docker-compose exec postgres psql -U jaxa -d englishlessons
```

Выполните SQL:
```sql
UPDATE users SET role = 'teacher' WHERE username = 'ваш_username';
```

## Очистка и пересоздание базы

Если нужно начать с чистого листа:

```bash
# Остановить и удалить все данные
docker-compose down -v

# Запустить заново
docker-compose up -d postgres
go run main.go
```

## Полезные команды

```bash
# Просмотр логов базы данных
docker-compose logs -f postgres

# Просмотр логов backend
docker-compose logs -f backend

# Остановка всех сервисов
docker-compose down

# Перезапуск
docker-compose restart
```

## Возможные проблемы

### База данных не запускается
```bash
# Проверьте что порт 5432 свободен
netstat -an | findstr 5432

# Или измените порт в docker-compose.yaml
```

### Backend не подключается к базе
- Убедитесь что база запущена: `docker-compose ps`
- Проверьте логи: `docker-compose logs postgres`
- Убедитесь что используете правильный connection string

### Ошибки миграций
- Остановите и удалите volumes: `docker-compose down -v`
- Запустите заново: `docker-compose up -d postgres && go run main.go`
