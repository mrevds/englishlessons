# English Lessons - Полная система изучения английского

Полнофункциональная система для изучения английского языка с уроками и игровыми тестами.

## 🚀 Быстрый старт

### Вариант 1: Docker (рекомендуется)

1. Запустите базу данных:
```bash
docker-compose up -d postgres
```

2. Запустите backend:
```bash
go run main.go
```

Или весь стек сразу:
```bash
docker-compose up -d
```

### Вариант 2: Локально

1. Установите зависимости:
```bash
go mod download
```

2. Убедитесь что PostgreSQL запущен (через Docker или локально)

3. Запустите сервер:
```bash
go run main.go
```

Backend будет доступен на `http://localhost:8080`

**Примечание:** При первом запуске автоматически выполняются миграции и создаются дефолтные уроки.

### Frontend (React + TypeScript)

1. Перейдите в директорию frontend:

```bash
cd frontend
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите dev сервер:

```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 📋 Функции

### Backend

- ✅ JWT аутентификация
- ✅ Регистрация (все по умолчанию студенты)
- ✅ Роли: student и teacher
- ✅ 10 уроков с вопросами и ответами
- ✅ Игровые тесты с проверкой 70%
- ✅ Блокировка следующих уроков
- ✅ Статистика и прогресс
- ✅ PostgreSQL база данных

### Frontend

- ✅ Красивый современный UI с Tailwind CSS
- ✅ Регистрация и авторизация
- ✅ 10 уроков с теорией
- ✅ Игровой формат тестов:
  - ⏱️ Таймер на каждый вопрос
  - 🎯 Прогресс-бар
  - ⭐ Система звезд за результат
  - 🎉 Анимации при завершении
- ✅ Статистика прогресса
- ✅ Адаптивный дизайн

## 🎮 Игровой формат тестов

- Таймер: 60 секунд на вопрос
- Визуальный прогресс-бар
- Анимации при выборе ответов
- Звезды за результат (1-5 звезд)
- Красивое отображение результатов
- Возможность перепрохождения

## 📚 Темы уроков

1. Present Simple
2. Past Simple
3. Future Simple
4. Present Continuous
5. Past Continuous
6. Present Perfect
7. Past Perfect
8. Future Perfect
9. Modal Verbs
10. Conditionals

## 🔧 Настройка

### База данных (Docker)

Локальная PostgreSQL запускается через Docker:

```bash
# Запуск только базы данных
docker-compose up -d postgres
```

**Параметры подключения:**
- Host: localhost (или postgres внутри Docker)
- Port: 5432
- Database: englishlessons
- User: jaxa
- Password: 11041104

**Connection string:**
```
postgres://jaxa:11041104@localhost:5432/englishlessons?sslmode=disable
```

При первом запуске автоматически:
- Создаются все таблицы (миграции)
- Создаются дефолтные уроки с тестами

### Изменение роли на учителя

Подключитесь к базе данных:

```bash
docker-compose exec postgres psql -U jaxa -d englishlessons
```

Выполните SQL:
```sql
UPDATE users SET role = 'teacher' WHERE username = 'ваш_username';
```

## 📝 API Endpoints

См. `README.md` в корне проекта для полной документации API.

## 🎨 Технологии

**Backend:**

- Go 1.23
- Gin
- GORM
- PostgreSQL
- JWT

**Frontend:**

- React 19
- TypeScript
- Vite
- Tailwind CSS
- React Router
- Axios

## 📦 Структура проекта

```
.
├── main.go                 # Точка входа Go приложения
├── internal/
│   ├── config/            # Конфигурация
│   ├── database/          # Подключение к БД и миграции
│   ├── handlers/          # HTTP handlers
│   ├── middleware/         # Middleware (auth, CORS)
│   └── models/            # Модели данных
└── frontend/              # React приложение
    ├── src/
    │   ├── api/          # API клиент
    │   ├── components/   # React компоненты
    │   ├── context/      # React Context
    │   ├── data/         # Данные уроков
    │   └── pages/        # Страницы
    └── ...
```

## 🎯 Логика работы

1. **Регистрация**: Все регистрируются как студенты
2. **Уроки**: Первый урок доступен сразу, остальные - после прохождения предыдущего на >=70%
3. **Тесты**: Можно перепроходить, сохраняется лучший результат
4. **Прогресс**: Отслеживается по каждому уроку отдельно

## 🚀 Готово к использованию!

Просто запустите backend и frontend, и система готова к работе!
